[package]
name = "common"
version = "0.1.0"
edition = "2024"

[dependencies]
zerocopy = { version = "0.8.24", features = ["derive"] }
embedded-io = { version = "0.6.0" }
embedded-io-async = { version = "0.6.1" }
defmt = { version = "1.0.1" }
